name: React Application Pipeline
run-name: Deploy by  @${{ github.actor }}

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      
jobs:
  demo:
    name: Demo Job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout ${{env.second}}  # set the environme variable nts 
        uses: actions/checkout@v4
        
      - name: Setup Node.js environments
        uses: actions/setup-node@v4
        with:
          node-version: 16.x

  
            
            

      
      # - name: Cache dependencies 
      #   uses: actions/cache@v4 
      #   with: 
      #     path: node_modules 
      #     key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
      

          

      # # 3. Install dependencies
      # - name: Install dependencies
      #   run: npm install

      # # 4. Build React app
      # # NOTE: CI=true is default in GitHub Actions.
      # # ESLint issues must be fixed in code (recommended).

      # - name: Build application
      #   run: CI=false npm run build


      
    

      # # 5. Upload build artifact
      # - name: Upload build artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: react-build-${{ github.run_id }}
      #     path: build
